//import { setTimeout } from "timers/promises";
/*var timePerFrameInMS = 8.33;
var MS = Date.now();

const eventarray = [];

const event1 = new MouseEvent("click");
eventarray.push(event1);

const event2 = new Event('keydown');
event2.keyCode = 40;
const event3 = new Event("keydown");
event3.keyCode = 37;
const event4 = new Event("keydown");
event4.keyCode = 39;

eventarray.push(event2); eventarray.push(event3); eventarray.push(event4);

var eventarray2 = eventarray.slice(1, 3);;

while (true) {
    if ((Date.now() - MS) >= (timePerFrameInMS * 10)) {
        console.log("in the loop");
        document.getElementById('game-start').dispatchEvent(event1);
        break;
    }

}

eventarray2.forEach(event => {
    window.dispatchEvent(event); 
});
*/